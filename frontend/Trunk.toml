# Trunk build config — see https://trunkrs.dev/configuration/
[build]
target = "index.html"
dist = "dist"
# The daemon serves the bundle from /static/*, so assets generated by
# trunk reference /static/<name>.<hash>.<ext>.
public_url = "/static/"
