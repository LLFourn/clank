APPROVE

This commit is only the `trinity` package version bump from `0.4.0` to `0.5.0` in `Cargo.toml` and `Cargo.lock`.

That matches the plan's dogfood/versioning requirement for shipping the later CLI phases. No issues found.
