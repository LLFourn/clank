APPROVE

This commit is only the package version bump from `0.3.0` to `0.4.0` in `Cargo.toml` and `Cargo.lock`.

The previous implementation revision (`26f807a`) already addressed the rewrite correctness concerns and passed the focused rewrite tests. I do not see any issue in this final bump commit.
