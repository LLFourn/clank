APPROVE

This commit is only the `trinity` package version bump from `0.6.0` to `0.7.0` in `Cargo.toml` and `Cargo.lock`.

The cache implementation itself was approved at `b4333d76122316fcd5d93bdfb6e2147433a325b1`; this shipping/version commit is fine.
